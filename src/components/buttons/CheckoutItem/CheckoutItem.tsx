

interface Props {
    imgUrl: string,
    index: number,
    price: number,
    total: number,
    type: string
}

export default function CheckoutItem({imgUrl, index, price, total, type}: Props) {
    return (
        <div className="">

        </div>

    )
}